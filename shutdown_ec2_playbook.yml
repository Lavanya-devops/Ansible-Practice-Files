---
- name : Shutting down the ubuntu instances only
  hosts: all
  become: true
  tasks:
    - name: shutting down ubuntu instances
      ansible.builtin.command: /sbin/shutdown -t now
      #ansible.builtin.debug:
      # var: ansible_facts
      when:
        ansible_facts['os_family'] == "Debian"
